\documentclass{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

% Required to use the command \text
\usepackage{amsmath}

\begin{document}

\section*{Text within an equation}

The following equation is the inverse function of $x$.

% The command \text{word or phrase} is used to display text wihtin a formula.
% The result is that the text rendered is not indented.
% The follwing example illustrates a common use of the command. Note a space is introduced before and after the word to properly render the formula.

\[f(x) = \frac{1}{x} \text{ where } x \neq 0 \]

\end{document}